

const config = {
    url_DB : "http://localhost:5000",
    url_Fr : "http://localhost:3000"

}

export default config;
// export {config,Login, Menu, User, Test, Logout, Document, Contact, Graph} ;
